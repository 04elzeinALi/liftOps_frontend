# LiftOps Frontend

React frontend for LiftOps, a bus transportation management system. Talks to the LiftOps Laravel API.

## Development

```bash
npm install
npm run dev
```

Requires the LiftOps backend running at the URL configured in `.env` (`VITE_API_URL`).
